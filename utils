# fatalError() - Exit with ...
# $1 Error message
# $2 Exit code
function fatalError() {
  if [ $# -eq 2 ]
  then 
    echo $1 >&2
    exit $2
  else 
    echo function ${FUNCNAME}: 2 arguments required
    exit 255
  fi
}
